#| DNS-SD, a Common Lisp library for Zeroconf service discovery.
   ------
   Basic tests. |#

(in-package #:dns-sd-tests)

(def-suite basic :in main
  :description "Basic functionality tests.")

(in-suite basic)


#+test (let ((*debug-on-error* t)) (run! 'basic))
